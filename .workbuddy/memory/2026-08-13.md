# 2026-08-13 工作日志

## 吉他调音器 · 平台决策与文档 v1.1

- 用户确认决策 D1：**目标平台 = 原生 Kotlin，仅 Android**（不采用 Flutter，无 iOS 需求）。
- 文档《吉他调音器开发文档.md》升级 v1.0 → v1.1：
  - 头部新增变更记录表；第 1 节加入"仅 Android"平台声明。
  - 第 2 节备选路线改写为「已确认不采用 Flutter」+「性能优化选项（Oboe / NDK）」；新增 **2.1 ADR 决策记录 D1**（理由：AudioRecord 零桥接直读 PCM、YIN 纯 Kotlin 可平移 NDK、无跨端需求时 Flutter 收益为零）。
  - 7.2 节补充 minSdk=26 建议与 Oboe(AAudio) 低延迟升级路径。
  - 第 9 节新增第 7 条：Compose 重组节流 ~30fps，DSP 留在采集子线程。
  - 第 14 节勾选确认平台项，新增待确认项「M4 延迟 >100ms 时是否启用 Oboe」。
- 技术要点备忘：E2 周期 ≈ 535 样本（44100/82.41）；帧长 2048 点 ≈ 46ms，端到端目标 <100ms；1 音分 @E2 ≈ 0.05 Hz。

## 吉他调音器 · 调弦范围确认（v1.2）

- 用户确认 D2：**支持吉他标准 + 降调（drop-D/drop-C）、尤克里里标准 + 降调（降半音/降全音）**。
- 文档升级 v1.1 → v1.2：§6 扩展为多调弦表（吉他 6 弦 EADGBE + 降调表；尤克里里 4 弦 GCEA + 降调表）；§4.3 fmin 依据更新（最低音 C2=65.41）；§8 UI 改两级选择（乐器→调弦→弦位）；§10 新增 Tunings.kt；§11 M3 验收扩展；§12.1 新增扩展验证表；§13 脚本支持多调弦；§14 勾选。
- 已用脚本实测全部 6 套调弦 26 个音：最大误差 0.07 音分（尤克里里 A4），fmin=55Hz/2048 点参数无需调整。
- 关键频率：尤克里里标准 GCEA = G4 392.00 / C4 261.63 / E4 329.63 / A4 440.00（re-entrant）；降半音 F#4 369.99 / B3 246.94 / D#4 311.13 / G#4 415.30；降全音 F4 349.23 / A#3 233.08 / D4 293.66 / G4 392.00。
- 注意：用户会在文档编辑后格式化 markdown（表格对齐、行尾双空格），再次 Edit 前需重新 Read。

## 吉他调音器 · 交付节奏与音频策略确认（v1.3）

- 用户确认 D3/D4：**先交付「核心 DSP 逻辑 + 单元测试」（M1 先行验收）**；**Oboe 暂不启用，列为 M4 条件触发项**（延迟 >100ms 或指针明显滞后时启用）。
- 文档升级 v1.2 → v1.3：§11 新增交付策略段（M1 先行 + Oboe 条件触发）、M1 验收细化；§14 全部 4 项待确认项已勾选完毕。
- 决策背景：端到端延迟预算 = 帧积累 46ms（2048 点，YIN 硬成本，Oboe 无法压缩）+ 系统采集缓冲 ~30ms（Oboe 可压至 ~12ms）+ DSP ~4ms + UI 16ms ≈ 96ms（当前）vs ~76ms（Oboe）；收益 ~20ms 但对调音感知无意义（容忍 ~100-150ms）。Oboe 代价：NDK/JNI、PitchDetector C++ 化、麦克风 input 低延迟依赖设备 HAL 不保证。

## 吉他调音器 · 开发环境现状盘点（2026-08-13）

- 机器现状：**JDK 21.0.10 LTS 已装**（满足 AGP 8.x 的 JDK 17+ 要求，可直接用）；**Android Studio / Android SDK / Gradle / ADB 均未安装**；JAVA_HOME 环境变量未设置（命令行 gradlew 构建前需补）。
- 待办：安装 Android Studio（最新稳定版，含 SDK Platform / Build-Tools / Platform-Tools / Emulator）；准备 Android 8.0+ 真机（minSdk=26，M2/M4 音频实测必须真机，模拟器麦克风不可靠）；国内网络需配 maven 镜像（阿里/腾讯）加速 AGP 依赖下载。
- 里程碑最小依赖：M1（纯 JVM DSP + JUnit 单测）仅需 JDK，无需 SDK/真机即可跑；M2 起需要 SDK + 真机。

## 吉他调音器 · 环境准备清单文档（2026-08-13）

- 创建《环境准备清单.md》（v1.0，项目根目录），内容：环境现状盘点（JDK 21 已装、Studio/SDK 未装）、组件版本要求、分里程碑最小环境（M1 仅需 JDK）、安装步骤（Studio + SDK + 真机 + 环境变量）、国内镜像配置（腾讯 Gradle 镜像 + 阿里云 Maven 镜像的 settings.gradle.kts 示例）、5 分钟验证清单、常见问题速查。

## 吉他调音器 · M1 交付完成（核心 DSP + 单元测试）

- **M1 验收 PASS**：6 套调弦 30 个音全部通过（合成正弦波 @44.1kHz、2048 点帧），最大误差 0.07 音分（尤克里里 A4），与文档 §12.1 Python 基准一致。
- **工程形态**（用户确认）：纯 Kotlin JVM 模块 `dsp/` 先行（Kotlin 2.0.21、JDK 21 toolchain、Gradle 8.10.2 wrapper 已配腾讯镜像），不依赖 Android SDK；M2 起建 `app/` Android 模块直接依赖 dsp。
- 交付文件：`dsp/src/main/kotlin/.../{dsp/PitchDetector.kt, music/NoteMapper.kt, music/Tunings.kt}`；测试 `dsp/src/test/kotlin/.../PitchDetectorTest.kt`（5 用例）+ `VerifyReport.kt`（`./gradlew :dsp:verifyReport` 输出逐音明细）；报告《DevLog/M1验收报告.md》。
- **用户已整理目录**：两份 md 文档已从项目根移入 `DevLog/` 子目录（《吉他调音器开发文档.md》《环境准备清单.md》）。
- **修正文档笔误**：《吉他调音器开发文档.md》§12.1 结论「26 个音」→「30 个音」（6+6+6+4+4+4）。
- 经验：文档 §4.4 示例代码是示意性（Kotlin `for` 循环内 `tau++` 不可编译、`cents` 需显式 Double 转换），实现时已按可编译版本落地；M1 构建坑：settings 设 `FAIL_ON_PROJECT_REPOS` 时模块内不能重复声明 repositories。

## 吉他调音器 · Android Studio + SDK 安装完成（2026-08-13）

- **Android Studio 2026.1.3（Quail 3）已安装**：`D:\Program Files\Android\Android Studio`（静默安装首次尝试即成功装到 D 盘，后续手动带 `/D=` 参数反复报 NSIS 退出码 199 是误导——实际已装好）。
- **Android SDK 已安装**（根目录 `C:\Android\Sdk`）：platform-tools 37.0.1、platforms;android-35、build-tools 35.0.0、cmdline-tools 15859902。
- **环境变量已设置**（用户级 setx）：`JAVA_HOME=D:\Java\jdk-21\jdk-21.0.10`、`ANDROID_HOME=C:\Android\Sdk`、`ANDROID_SDK_ROOT=C:\Android\Sdk`（新终端生效）。
- 下载渠道备忘：Google 官方 CDN `edgedl.me.gvt1.com`（302 到 gvt1-cn.com 国内节点）可达且快（~3.4MB/s）；`dl.google.com/android/repository` 直连可用（sdkmanager 默认源即可）；winget 源可查版本（`Google.AndroidStudio`）。
- 剩余待办：**真机**（Android 8.0+，M2 前置）；《环境准备清单.md》已更新至 v1.1 标记安装状态。
- 经验：NSIS 安装器静默安装排查时，先用 `winget list` 判断是否已装，别重复执行安装器；Windows 原生 curl 用 `C:/` 风格路径而非 `/c/`。
