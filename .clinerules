# Memory Bank 持续开发记忆规则

在本项目中执行任何分析、实现、调试、修复、重构或复盘任务前，必须优先检查并维护 `memory_bank/` 目录，使其成为该项目的持续开发记忆层，而不是一次性说明文档。

## 一、必备目录与文件

项目根目录下维护 `memory_bank/`，默认包含以下文件：

### 每次任务前强制读取
- `memory_bank/projectbrief.md`
- `memory_bank/productContext.md`
- `memory_bank/activeContext.md`
- `memory_bank/progress.md`

### 按需读取
- `memory_bank/bugList.md`
- `memory_bank/optimizationRoadmap.md`

若 `memory_bank/` 不存在、文件缺失、内容明显过期、职责边界混乱或文档膨胀，必须先补齐或整理，再继续当前任务。

## 二、文件职责约束

### `projectbrief.md`
记录项目稳定基础信息：项目名称、项目定位、当前版本重点、基本信息、核心模块或处理链路、技术栈。

### `productContext.md`
记录业务背景、主要使用者、核心需求、关键约束、当前产品关注点。

### `activeContext.md`
记录当前开发主线、重点模块或问题、当前稳定结论、关键文件、当前风险与下一步。

### `progress.md`
记录已完成的基础能力、最近完成的关键事项、当前已确认结论、下一步建议。

### `bugList.md`
记录需持续跟踪的长期问题、缺陷或架构隐患。

### `optimizationRoadmap.md`
记录项目级优化方向、专题分析、优先级和验证路径。

## 三、初始化与补齐规则

1. 若项目尚未建立 `memory_bank/`，必须先创建目录和基础文件。
2. 初次建立时，必须基于项目真实内容填充，而不是只生成空模板。
3. 建立前应至少阅读项目的 README、主要源码目录、配置文件及已有设计文档。
4. 初始化内容至少应覆盖：项目目标、业务背景、技术栈、核心模块或处理链路、当前开发重点、已知问题与风险、已完成阶段成果。

## 四、任务过程中的同步维护规则

1. 每次开始新任务前，先读取四个核心文件。
2. 仅当任务直接涉及缺陷追踪、问题复盘或架构问题整理时，读取和更新 `bugList.md`。
3. 仅当任务直接涉及优化规划、方案设计、路线图整理或中长期改进建议时，读取和更新 `optimizationRoadmap.md`。
4. 在分析、实现、调试、修复、重构或复盘过程中，只要取得阶段性进展，就应同步更新相关 memory bank 文件。
5. 任务完成时，必须回写本次任务带来的关键信息，包括当前处理内容、重要决策、已完成事项与后续待办。

## 五、按文件的更新触发条件

### 优先更新 `activeContext.md` 的情形
- 当前开发主线发生变化
- 当前重点模块或问题发生变化
- 出现新的稳定结论
- 当前风险判断发生变化
- 下一步工作重点发生调整

### 优先更新 `progress.md` 的情形
- 某项功能或治理工作形成阶段性闭环
- 出现可沉淀的稳定成果
- 新的已确认结论形成
- 下一步建议发生变化

### 更新 `projectbrief.md` / `productContext.md` 的情形
- 对项目定位、技术栈、业务背景或产品约束的理解发生变化
- 项目基础画像需要修正或补充

### 更新 `bugList.md` 的情形
- 新发现长期问题
- 旧问题状态变化
- 风险等级变化
- 整改建议更新

### 更新 `optimizationRoadmap.md` 的情形
- 形成新的优化专题
- 某方向优先级发生变化
- 新的验证路径或规划建议成熟

## 六、反膨胀与反流水账规则

1. `memory_bank/` 的核心目标是沉淀稳定记忆，不是保存完整任务过程。
2. 核心文件优先记录：稳定背景、当前主线、阶段成果、稳定结论、风险、下一步。
3. 大量排查过程、实验细节、逐步推导应优先下沉到 `docs/`、`DesignLog/` 或专题文档。
4. 若发现内容重复、职责混写、冗长难扫或文档膨胀，必须主动压缩与重构。

## 七、Git 变更后的同步规则

在执行 `commit`、`pull`、合并分支或引入较大代码改动后，必须检查是否需要同步更新 `memory_bank/`：

- 当前主线是否变化；
- 阶段成果是否增加；
- 风险与下一步是否变化；
- 项目定位、背景或技术栈理解是否变化。

若有影响，必须同步回写相关文档。

## 八、质量目标

维护后的 `memory_bank/` 应满足以下目标：

1. 新 agent 能在短时间内理解项目背景和当前重点；
2. 各文件职责清晰、边界稳定；
3. 核心信息低冗余、可扫描；
4. 支持多轮开发任务续接；
5. 支持阶段复盘、问题追踪和长期优化规划。

# Windows CLI / 大文件 / Git 命令稳定性规则

本项目默认 shell 为 `C:\windows\system32\cmd.exe`。在 Windows / VSCode / Cline 工具调用环境中执行 CLI 分析命令时，必须优先保证**解析稳定、输出可控、非交互**，避免因长命令、复杂引号、大文件扫描或 Git 分页器导致工具调用表现为 `Pending`、`skipped` 或“卡住”。

执行规则：

1. **默认短命令、分步执行**：每条命令只完成一个目标；先做存在性检查、样本预览或范围缩小，再做正式统计与汇总。
2. **禁止高风险内联长命令**：禁止直接执行复杂的 `python -c`、`py -c`、`powershell -Command`、`node -e`；凡是包含复杂引号、控制流、多字段批量访问，或预计超过 300 个字符的命令，必须改为“先写临时脚本，再执行脚本”。
3. **大文件分析必须保守**：处理 CSV、JSON、日志、大文本时，默认逐行读取；禁止整文件读入内存后筛选。若仅做排查或预览，必须限制输出条数，命中上限立即退出。
4. **避免单条命令职责过多**：禁止把“大文件读取 + 多组统计 + 大量打印 + 复杂控制流”塞进一条命令，避免 `cmd.exe` 解析风险与输出阻塞叠加。
5. **长时间无响应时先排查工具链问题**：优先检查 `cmd.exe` 引号/换行解析异常、输出过大、命令职责过多等非算法因素，不要立即判定为业务代码死循环。
6. **审批命令优先可读和低转义**：提交审批时优先使用短、直白、低转义负担的命令；若命令被 Skip、取消或审批失败，必须立即改用更短、更安全的替代方案。
7. **Git 命令必须禁用分页器**：执行 `git log`、`git show`、`git diff`、`git blame` 等多行输出命令时，默认使用 `git --no-pager ...` 或 `git -c core.pager=cat ...`。
8. **Git 查询必须限制输出规模**：优先使用 `--max-count`、`--name-only`、`--stat`、路径过滤等参数；涉及多个 commit 时，先列 commit，再按单个 commit 分步查看，禁止一次性拉取大输出 patch。
9. **出现 Git 挂起时按分页器问题处理**：若终端停在分页输出，优先按 `q` 退出；若工具调用出现 `Pending`、`skipped` 或输出停住，应立即改用 `--no-pager` 的短命令重试。

目标：避免 Cline 在 Windows 环境下因命令解析、输出链路或 Git 分页器问题出现假死、审批卡住或大文件扫描阻塞。